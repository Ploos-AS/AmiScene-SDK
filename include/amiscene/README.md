# AmiScene assembler includes

This tree is the public assembly-facing surface: hardware definitions, small macros and interfaces for reusable routines. 68000/OCS compatibility is the default unless a file explicitly declares another target.
